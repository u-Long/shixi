## 梳理优化相关参数和模型

### 特征：
1. 特征是否截面归一化。
2. 因子选择

### 标签：
1. label制作与选取。

### iTransformer模型：
1. GNN、Attention处理N股票空间维度
2. F维度做完注意力以后的处理，现在是meanpool
3. loss设计

### Something Important
根据ic高低，看是因子还是信号。就是由原始数据，产出信号，ic不高 0.02-0.04，那就是因子；输入是因子，产出ic比较高0.04以上？0.1几，可以直接用到投资组合优化的，就是信号；原始数据直接产出高ic，也是信号。
但是由于nn有随机性，重训不稳定性，不是稳定的数学公式。所以现在nn因子机器学习因子比较少用，或者说离不开传统因子。固定计算方式，可解释性高。

纯时序不用考虑N这个维度，就是B,T,F。B相当于在N里面取嘛。现在其实就相当于batchsize=N的情况。
只有stgnn才需要考虑N这个维度。那么此时B>1，就要考虑N的大小和顺序的一致性了。基础的解决思路就是pad到最大，固定index。


全股训练hs300测试
linear共享权重
回测cache